DATABASE_URL="postgresql://johndoe:randompassword@127.0.0.1:5433/mydb?schema=public"
PORT=4000
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# Firebase client configuration. This is useful for reference, but it is not
# enough for backend Firebase Admin token verification.
FIREBASE_API_KEY="AIzaSyARQmqdNHB5EW99lQtrhevPvbHc_J2AFyM"
FIREBASE_AUTH_DOMAIN="afya-mnyama-digital.firebaseapp.com"
FIREBASE_PROJECT_ID="afya-mnyama-digital"
FIREBASE_STORAGE_BUCKET="afya-mnyama-digital.firebasestorage.app"
FIREBASE_MESSAGING_SENDER_ID="116773884327"
FIREBASE_APP_ID="1:116773884327:web:af3de99477ff13fc592682"
FIREBASE_MEASUREMENT_ID="G-Y208VH8N41"

# Required for /api/auth/exchange with real Firebase ID tokens.
# Use a Firebase Admin service account JSON from the same Firebase project
# that issues the MnyamaCheck web app tokens.
# FIREBASE_CREDENTIALS='{"type":"service_account","project_id":"..."}'
FIREBASE_CREDENTIALS_FILE="./afya-mnyama-digital-firebase-adminsdk-fbsvc-280bd6e441.json"

# Seed an unrelated demo product catalog (fertilizers, pesticides, etc.) —
# useful for local exploration, not for a real Mnyama Shop-linked database.
SEED_BUILTIN_CATALOG=false

# Import Mnyama Shop products from Firestore (afya-mnyama-digital / products).
# Each doc must have agrovet_catalog.default_variant_id (Firebase UUID ->
# ProductVariant.id) and a variants[] array (one entry per variant).
SEED_FIREBASE_PRODUCTS=false
FIREBASE_PRODUCTS_COLLECTION=products
FIREBASE_PUBLISHED_ONLY=true
# Optional overrides for Firestore REST reads (defaults to FIREBASE_* above)
# MNYAMA_SHOP_FIREBASE_API_KEY=
# MNYAMA_SHOP_FIREBASE_PROJECT_ID=
